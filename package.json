{
  "name": "knowledge-database-prototype",
  "version": "1.0.0",
  "description": "Knowledge Database (Lessons Learned) — Standalone Prototype",
  "scripts": {
    "start": "concurrently --names backend,frontend --prefix-colors blue,green \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npx ng serve --proxy-config proxy.conf.json --open",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend:only": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
